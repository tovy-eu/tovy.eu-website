rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients no longer write to Firestore — all intake data goes to tovy-os.
    // Admin SDK (Firebase extensions, server-side) bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
